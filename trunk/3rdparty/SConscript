Import('env')

GTEST_DIR = 'googletest-1.4.0/'
GTEST_SRC = [
   GTEST_DIR + 'src/gtest-all.cc',
   GTEST_DIR + 'src/gtest_main.cc',
]
gtest = env.Clone()
gtest.AppendUnique(CPPPATH = [GTEST_DIR])
gtest.AppendUnique(CPPPATH = [GTEST_DIR + 'include'])
gtest.Library('googletest', GTEST_SRC)
